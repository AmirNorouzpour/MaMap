<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.mamap.app">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="ir.mamap.app.Mamap"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ir.mamap.app.SupportActivity"></activity>
        <activity android:name="ir.mamap.app.RulesActivity" />
        <activity android:name="ir.mamap.app.UserDataEditActivity" />
        <activity android:name="ir.mamap.app.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ir.mamap.app.ExceptionActivity" />
        <activity android:name="ir.mamap.app.OnEnterActivity" />
        <activity android:name="ir.mamap.app.LoginActivity" />
        <activity android:name="ir.mamap.app.VerificationActivity" />
        <activity android:name="ir.mamap.app.RegisterActivity" />
        <activity android:name="ir.mamap.app.MenuActivity" />

        <service android:name="ir.mamap.app.FirebaseService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name="ir.mamap.app.LocationService"
            android:enabled="true" />

        <receiver
            android:name="ir.mamap.app.RestarterBroadcastReceiver"
            android:enabled="true"
            android:exported="true"
            android:label="RestartServiceWhenStopped">
            <intent-filter>
                <action android:name="ac.in.ActivityRecognition.RestartSensor" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyAvvR6vDDafdq7F8SbHjKMiLrvRYUtFrMI" />

        <activity android:name="ir.mamap.app.AddUserActivity" />
        <activity android:name="ir.mamap.app.MessageListActivity" />
    </application>

</manifest>